@{
  Layout = "_Layout";
}

@using Factory.Models;

<h1>Engineers</h1>

@if (@Model.Count == 0)
{
  <h3>No Engineers have been added yet!</h3>
} 

<ul>
  @foreach(Engineer engineer in Model)
  {
    <li>@Html.ActionLink($"{engineer.Name}", "Details", new { id = engineer.EngineerId })</li>
  }
</ul>

@* <p>@Html.ActionLink("Add new engineer", "Create")</p>
<p>@Html.ActionLink("Home", "Index", "Home")</p> *@
@using(@Html.BeginForm("Create", "Engineers"))
{
  <input type="submit" value="New Engineer" class="btn btn-primary">
}
<br>
@using(@Html.BeginForm("Index", "Engineers"))
{
  <input type="submit" value="Back to Engineers" class="btn btn-primary">
}
<br>